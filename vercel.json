{
  "builds": [
    {
      "src": "api/index.ts",
      "use": "@vercel/node"
    }
  ],
  "functions": {
    "api/*.ts": {
      "runtime": "nodejs18.x",
      "maxDuration": 30
    }
  },
  "buildCommand": "npm install canvas node-fetch && npm run build",
  "installCommand": "apt-get update && apt-get install -y libcairo2-dev libpango1.0-dev libjpeg-dev libgif-dev librsvg2-dev"
}
